import { DisabledForRoleDirective } from './disabled-for-role.directive';

describe('DisabledForRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledForRoleDirective();
    expect(directive).toBeTruthy();
  });
});
